
  # Personal Portfolio Website Design

  This is a code bundle for Personal Portfolio Website Design. The original project is available at https://www.figma.com/design/JV5dM5RMbTWKhdEqqpIQdP/Personal-Portfolio-Website-Design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
  if show  react problem install - npm install --save-dev @types/react @types/react-dom

  typescripts version checj - npm tsx -v